[project]
name = "hospital-env"
version = "2.0.0"
description = "Hospital Resource Allocation RL Environment"
requires-python = ">=3.10"

[project.dependencies]
fastapi = ">=0.104.0"
uvicorn = ">=0.24.0"
requests = ">=2.25.0"
pydantic = ">=2.0.0"
openai = ">=1.0.0"
